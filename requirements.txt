# Direct runtime dependencies for the dynaphos package and command-line tools.
# Install the package itself with `pip install -e .`.
matplotlib>=3.8
numpy>=1.26
opencv-python>=4.9
pillow>=10.0
PyYAML>=6.0
scipy>=1.12
torch>=2.2

# Development, tests, and notebooks.
ipykernel>=6.29
ipywidgets>=8.1
jupyter>=1.0
pytest>=8.0
